# Güvenli, rastgele bir string oluştur (örn: openssl rand -hex 32)
SECRET_KEY=buraya-guclu-bir-secret-key-girin

# Veritabanı (Railway'de otomatik doldurulur)
DATABASE_URL=sqlite:///./app.db

# Port (Railway otomatik ayarlar)
PORT=8000
